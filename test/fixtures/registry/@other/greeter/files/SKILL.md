---
name: greeter
version: 0.1.0
description: A different skill that happens to share the slug "greeter".
---

# Greeter (other)

Note that this declares `name: greeter` too — which is why aliasing the directory
would not separate the two for a loader that keys on frontmatter.
